extends layout

block content
  h1= title
  
  ul
    each owner in owner_list
      li 
        a(href=owner.url) <span class="green-text"> <h5>#{owner.name}</h5>
        |  (#{owner.year_founded})

    else
      li <span class="green-text"> There are no owners.